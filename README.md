# HN Enterprises Mobile App

Expo Router scaffold for the supervisor mobile app.

This folder intentionally contains reusable structure only:

- app shell and providers
- theme tokens
- generic UI primitives
- reusable hooks
- shared types and formatting helpers

Feature screens for attendance, customer workspace, LMC, evidence capture and DPR can be added on top of this structure.
